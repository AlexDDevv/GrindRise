import { PlaceholderScreen } from '../../../components/PlaceholderScreen';

export function ProgressionScreen() {
  return (
    <PlaceholderScreen
      title="Progression"
      description="Profil, classe, courbe de niveaux (`level_thresholds`), historique d'XP et analytics de stagnation."
    />
  );
}
